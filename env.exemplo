# Variáveis de ambiente Django

SECRET_KEY=
DEBUG=True

# Variaveis de Email

EMAIL_HOST=smtp.gmail.com
EMAIL_HOST_USER=EMAIL DE USUARIO
EMAIL_HOST_PASSWORD=SENHA DO APP
EMAIL_PORT=587
EMAIL_USE_TLS=True
DEFAULT_FROM_EMAIL=EMAIL DE USUARIO
SERVER_EMAIL=DEFAULT_FROM_EMAIL
EMAIL_BACKEND=django.core.mail.backends.smtp.EmailBackend
CLIENT_ID=ID DO CLIENTE
CLIENT_SECRET=CLIENTE SECRETO
